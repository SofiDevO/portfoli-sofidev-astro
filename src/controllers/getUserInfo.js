// getUserInfo.js
import userData from "@src/data/userData";

// Función para obtener la información del usuario por índice
const getUserInfo = userData.map((data) => {
  return data;
});


export default getUserInfo[0];
